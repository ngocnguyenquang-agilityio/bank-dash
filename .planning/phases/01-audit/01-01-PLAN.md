---
phase: 01-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-audit/VALIDATION-AUDIT.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02

must_haves:
  truths:
    - "Developer can read a single reference listing every file that performs card number validation"
    - "Developer can see the specific rules each location enforces (length limit, character type, format pattern)"
    - "Each entry names the file, the mechanism, and the rule — no entry is ambiguous"
  artifacts:
    - path: ".planning/phases/01-audit/VALIDATION-AUDIT.md"
      provides: "Complete card number validation audit document"
      contains: "## Client-Side Validation"
    - path: ".planning/phases/01-audit/VALIDATION-AUDIT.md"
      provides: "Server-side validation documentation"
      contains: "## Server-Side Validation"
  key_links:
    - from: "VALIDATION-AUDIT.md"
      to: "bank-dash-app/src/types/card.ts"
      via: "Documents CardSchema and CardFormSchema validation rules"
      pattern: "CardSchema|CardFormSchema"
    - from: "VALIDATION-AUDIT.md"
      to: "bank-dash-server/src/api/card/content-types/card/schema.json"
      via: "Documents Strapi regex validation on number field"
      pattern: "schema\\.json"
---

<objective>
Produce a single reference document that lists every location in the codebase where card number validation occurs, what mechanism each location uses, and what rule it enforces.

Purpose: Phase 2 (Implement) needs a clear map of what to change and what to remove. Without this audit, changes risk missing a validation location or misunderstanding a rule.
Output: `.planning/phases/01-audit/VALIDATION-AUDIT.md` — the definitive reference for Phase 2 work.
</objective>

<execution_context>
@D:/bank-dash/.claude/get-shit-done/workflows/execute-plan.md
@D:/bank-dash/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all card number validation locations and write VALIDATION-AUDIT.md</name>
  <files>.planning/phases/01-audit/VALIDATION-AUDIT.md</files>
  <read_first>
    bank-dash-app/src/types/card.ts
    bank-dash-app/src/constants/regex.ts
    bank-dash-app/src/components/AddCardModal/AddCardModal.tsx
    bank-dash-server/src/api/card/content-types/card/schema.json
    bank-dash-app/src/services/cards.ts
    bank-dash-app/src/lib/utils.ts
    bank-dash-app/src/lib/errors/handleApiError.ts
  </read_first>
  <action>
Create the file `.planning/phases/01-audit/VALIDATION-AUDIT.md` with the following exact structure and content. The executor must read every file in read_first to confirm the details below are still accurate, then write the document.

The document MUST contain these sections:

**Header:**
```
# Card Number Validation Audit
**Date:** {current date}
**Phase:** 01-audit (AUDIT-01, AUDIT-02)
**Scope:** All files that validate, constrain, or format the card number field
```

**Section 1 — Client-Side Validation (4 locations):**

Location 1: `bank-dash-app/src/constants/regex.ts`
- Mechanism: Exported constant `REGEX.CARD_NUMBER`
- Pattern: `/^\d{4}-?\d{4}-?\d{4}-?\d{4}$/`
- Rule: Requires exactly 16 digits, optionally separated by dashes into 4 groups of 4. Enforces digits-only (no letters or special characters).
- Used by: `CardSchema` in `src/types/card.ts`

Location 2: `bank-dash-app/src/types/card.ts` — `CardSchema` (lines 24-28)
- Mechanism: Effect-TS `Schema.pattern(REGEX.CARD_NUMBER, ...)` on the `number` field
- Rule: Validates API response data matches the 16-digit (with optional dashes) pattern. Error message: "Invalid card number format"
- Used by: `CardsResponseSchema` which wraps `CardSchema` in an array — used when parsing GET /cards responses
- Note: This is response validation, not input validation. It validates data coming FROM the server.

Location 3: `bank-dash-app/src/types/card.ts` — `CardFormSchema` (lines 51-53)
- Mechanism: Effect-TS `Schema.minLength(1)` + `Schema.maxLength(19)` on the `cardNumber` field
- Rule: Requires at least 1 character (non-empty). Maximum 19 characters (16 digits + 3 dashes in formatted form `1234-5678-9012-3456`). Error messages: "Card number is required" / "The maximum number is 16 digits"
- Used by: `AddCardModal` via react-hook-form `effectTsResolver(CardFormSchema)` — this is the form-level validation
- Note: This does NOT enforce digits-only at the schema level. The digits-only enforcement happens via the `formatCardNumber` function in AddCardModal.

Location 4: `bank-dash-app/src/components/AddCardModal/AddCardModal.tsx` — `formatCardNumber` function (lines 84-95)
- Mechanism: JavaScript string manipulation in the `onChange` handler of the card number input
- Rule: (a) Strips all non-digit characters via `value.replace(/\D/g, '')`. (b) Truncates to 16 digits via `digitsOnly.slice(0, 16)`. (c) Inserts dashes every 4 digits via regex replace `(/(\d{4})(?=\d)/g, '$1-')`.
- Used by: The `<Controller name="cardNumber">` `onChange` handler (line 227)
- Note: This is the real-time input filter. It prevents non-digits from ever reaching the form state and caps length at 16 digits.

**Section 2 — Server-Side Validation (1 location):**

Location 5: `bank-dash-server/src/api/card/content-types/card/schema.json` — `number` attribute (lines 28-31)
- Mechanism: Strapi schema `regex` property on the `number` field
- Pattern: `"^[0-9]{16}$"`
- Rule: Requires exactly 16 digits (0-9), no dashes, no other characters. Also has `"required": true` so the field cannot be null/empty.
- Triggered when: Strapi processes POST /cards or PUT /cards/:id requests — Strapi validates the request body against this schema before saving to the database.
- Note: The frontend strips dashes before submission (`data.cardNumber.replace(/-/g, '')` in AddCardModal line 116), so the value arriving at Strapi should be 16 raw digits matching this regex.

**Section 3 — Data Flow Summary:**

Describe the validation chain for a card number from user input to database:

1. User types in AddCardModal card number field
2. `formatCardNumber` strips non-digits and caps at 16 digits (Location 4)
3. react-hook-form validates via `CardFormSchema` — checks minLength(1) and maxLength(19) (Location 3)
4. On submit, `AddCardModal.onSubmit` strips dashes: `data.cardNumber.replace(/-/g, '')`
5. `cards.ts` `addCard` sends raw digits to POST /cards as `number` field
6. Strapi validates `number` field against regex `^[0-9]{16}$` (Location 5)
7. If valid, saved to SQLite database

When reading cards back:
1. GET /cards returns `number` field (16 raw digits from DB)
2. `CardsResponseSchema` validates response via `CardSchema.number` using `REGEX.CARD_NUMBER` (Location 2)

**Section 4 — Observations for Phase 2:**

List these observations:
- The `formatCardNumber` function in AddCardModal is the primary real-time guard (prevents non-digits, caps at 16)
- `CardFormSchema.maxLength(19)` is a secondary guard (allows the formatted string with dashes)
- `CardFormSchema` does NOT enforce digits-only or the exact 16-digit pattern — it relies on `formatCardNumber` for that
- The Strapi `regex: "^[0-9]{16}$"` is the only server-side validation — it is redundant with the client-side formatting
- `CardSchema` validates response data, not input — it exists to ensure the API returns expected shapes
- `REGEX.CARD_NUMBER` in `constants/regex.ts` is only consumed by `CardSchema` (response validation), not by form input validation
- The `handleApiError` utility in `src/lib/errors/handleApiError.ts` parses Strapi `ValidationError` responses into field-level errors, so if the server regex rejects a card number, the error would surface as a field error — but with client formatting in place, this path is rarely hit

**Section 5 — File Index:**

A table with columns: File | Line(s) | Mechanism | Rule Summary | Side (Client/Server)

| File | Line(s) | Mechanism | Rule Summary | Side |
|------|---------|-----------|--------------|------|
| `bank-dash-app/src/constants/regex.ts` | 2 | Regex constant | 16 digits, optional dashes in 4-4-4-4 groups | Client |
| `bank-dash-app/src/types/card.ts` | 24-28 | Effect Schema.pattern | Response must match CARD_NUMBER regex | Client |
| `bank-dash-app/src/types/card.ts` | 51-53 | Effect Schema.minLength/maxLength | Non-empty, max 19 chars (16 digits + 3 dashes) | Client |
| `bank-dash-app/src/components/AddCardModal/AddCardModal.tsx` | 84-95 | JS string replace + slice | Strip non-digits, cap at 16 digits, insert dashes | Client |
| `bank-dash-server/src/api/card/content-types/card/schema.json` | 28-31 | Strapi schema regex | Exactly 16 digits [0-9], required field | Server |
  </action>
  <verify>
    <automated>grep -c "## Client-Side Validation" D:/bank-dash/.planning/phases/01-audit/VALIDATION-AUDIT.md && grep -c "## Server-Side Validation" D:/bank-dash/.planning/phases/01-audit/VALIDATION-AUDIT.md && grep -c "schema.json" D:/bank-dash/.planning/phases/01-audit/VALIDATION-AUDIT.md && grep -c "formatCardNumber" D:/bank-dash/.planning/phases/01-audit/VALIDATION-AUDIT.md && grep -c "CardFormSchema" D:/bank-dash/.planning/phases/01-audit/VALIDATION-AUDIT.md && grep -c "REGEX.CARD_NUMBER" D:/bank-dash/.planning/phases/01-audit/VALIDATION-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION-AUDIT.md exists at `.planning/phases/01-audit/VALIDATION-AUDIT.md`
    - File contains `## Client-Side Validation` section header
    - File contains `## Server-Side Validation` section header
    - File contains `## Data Flow Summary` section header
    - File contains `## Observations for Phase 2` section header
    - File contains `## File Index` section header
    - File references all 5 validation locations:
      - `bank-dash-app/src/constants/regex.ts` with pattern `/^\d{4}-?\d{4}-?\d{4}-?\d{4}$/`
      - `bank-dash-app/src/types/card.ts` with `CardSchema` and `Schema.pattern`
      - `bank-dash-app/src/types/card.ts` with `CardFormSchema` and `Schema.maxLength(19)`
      - `bank-dash-app/src/components/AddCardModal/AddCardModal.tsx` with `formatCardNumber`
      - `bank-dash-server/src/api/card/content-types/card/schema.json` with `"regex": "^[0-9]{16}$"`
    - File Index table contains exactly 5 rows of data (plus header)
    - The document does NOT contain placeholder text like "TBD", "TODO", or "[fill in]"
  </acceptance_criteria>
  <done>
    A single markdown file exists that a developer can read to know every location where card number validation exists, what mechanism each uses, and what rule each enforces. All 5 locations documented. All rules specified with exact patterns/limits. No ambiguity.
  </done>
</task>

</tasks>

<verification>
After task completion, verify:
1. `VALIDATION-AUDIT.md` exists and is non-empty
2. All 5 validation locations are documented (grep for each file path)
3. Both client-side and server-side sections are present
4. The data flow section traces input through all validation steps
5. The file index table has 5 entries
</verification>

<success_criteria>
- Developer can open a single file and immediately see all card number validation locations
- Each location specifies: file path, line numbers, mechanism (regex/schema/JS function), and exact rule
- The document distinguishes between input validation (form), response validation (API), and server validation (Strapi schema)
- Phase 2 planner can use this document to identify what to modify (client) and what to remove (server)
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit/01-01-SUMMARY.md`
</output>
