---
phase: 02-implement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bank-dash-server/src/api/card/content-types/card/schema.json
autonomous: true
requirements: [SERVER-01, SERVER-02]

must_haves:
  truths:
    - "Strapi backend does not reject card numbers based on format — only requires the field to be non-empty"
    - "Card creation via POST /cards still succeeds with a 16-digit string after removing the regex"
  artifacts:
    - path: "bank-dash-server/src/api/card/content-types/card/schema.json"
      provides: "Card content-type without regex validator on number field"
      contains: '"required": true'
  key_links:
    - from: "bank-dash-app/src/services/cards.ts"
      to: "bank-dash-server/src/api/card/content-types/card/schema.json"
      via: "POST /cards request with number field"
      pattern: "number.*required.*true"
---

<objective>
Remove the regex validator from the Strapi card content-type schema so the backend no longer enforces card number format rules.

Purpose: Satisfies SERVER-01 (no server-side card number validation) and SERVER-02 (card creation still works after removal).
Output: Updated schema.json with regex removed, existing service tests still green.
</objective>

<execution_context>
@.planning/phases/02-implement/02-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit/VALIDATION-AUDIT.md
@.planning/phases/02-implement/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From bank-dash-server/src/api/card/content-types/card/schema.json — current "number" attribute (lines 27-31):
```json
"number": {
  "type": "string",
  "required": true,
  "regex": "^[0-9]{16}$"
}
```

From bank-dash-app/src/services/cards.ts — addCard sends the card number as the "number" field:
The client strips dashes before submission: `data.cardNumber.replace(/-/g, '')`, producing a raw 16-digit string sent as `number` to POST /cards.

From bank-dash-app/src/services/test/cards.test.ts — existing addCard test:
```typescript
const result = await addCard('clerk-123', {
  cardNumber: '1234567890123456',
  nameOnCard: 'Test User',
  expiration: '2027-12',
  isPhysical: true,
  address: '123 Main St',
  balance: '1000',
});
expect(result.success).toBe(true);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove regex validator from Strapi card schema</name>
  <files>bank-dash-server/src/api/card/content-types/card/schema.json</files>
  <read_first>
    - bank-dash-server/src/api/card/content-types/card/schema.json
  </read_first>
  <action>
Edit `bank-dash-server/src/api/card/content-types/card/schema.json`. In the `"number"` attribute object (currently at lines 27-31), remove the `"regex": "^[0-9]{16}$"` key-value pair. Keep `"type": "string"` and `"required": true`.

**Before (lines 27-31):**
```json
"number": {
  "type": "string",
  "required": true,
  "regex": "^[0-9]{16}$"
}
```

**After:**
```json
"number": {
  "type": "string",
  "required": true
}
```

Do NOT modify any other attribute in schema.json. Do NOT remove `"required": true`. Do NOT change `"type": "string"`.

After the edit, verify by reading the file to confirm the regex line is gone and the JSON is valid.
  </action>
  <verify>
    <automated>cd bank-dash-server && node -e "const s = require('./src/api/card/content-types/card/schema.json'); const n = s.attributes.number; if (n.regex) { console.error('FAIL: regex still present'); process.exit(1); } if (!n.required) { console.error('FAIL: required missing'); process.exit(1); } if (n.type !== 'string') { console.error('FAIL: type changed'); process.exit(1); } console.log('PASS: number field has type=string, required=true, no regex');"</automated>
  </verify>
  <acceptance_criteria>
    - schema.json does NOT contain the string `"regex"`
    - schema.json still contains `"required": true` inside the `"number"` attribute
    - schema.json still contains `"type": "string"` inside the `"number"` attribute
    - schema.json is valid JSON (node can require it without error)
    - No other attributes in schema.json are modified
  </acceptance_criteria>
  <done>The "number" attribute in Strapi schema.json has type "string" and required true, with no regex validator. SERVER-01 is satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Verify existing card service tests still pass after schema removal</name>
  <files>bank-dash-app/src/services/test/cards.test.ts</files>
  <read_first>
    - bank-dash-app/src/services/test/cards.test.ts
  </read_first>
  <action>
Run the existing card service tests to confirm that removing the Strapi regex does not break the addCard flow at the client level. The service tests mock fetch and verify the request/response contract — they must still pass since the client sends a 16-digit string regardless.

Execute: `cd bank-dash-app && npx jest src/services/test/cards.test.ts --no-coverage`

This task does NOT modify any files. It only runs existing tests to prove SERVER-02 (removing server-side validation does not break card creation or update flows).

If any test fails, investigate the failure. The expected outcome is all tests pass because:
1. `addCard` strips dashes and sends a raw 16-digit string
2. The mock responses do not exercise Strapi validation (they are unit tests)
3. The removal of the regex only affects Strapi's runtime validation, not the client's request format

Also run the full test suite: `cd bank-dash-app && npm run test` to confirm no regression.
  </action>
  <verify>
    <automated>cd bank-dash-app && npx jest src/services/test/cards.test.ts --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest src/services/test/cards.test.ts --no-coverage` exits 0
    - The "creates a card successfully" test in addCard describe block passes
    - `npm run test` in bank-dash-app exits 0 (full suite green)
  </acceptance_criteria>
  <done>All card service tests pass after schema change. Full test suite green. SERVER-02 is satisfied.</done>
</task>

</tasks>

<verification>
1. `cd bank-dash-server && node -e "const s = require('./src/api/card/content-types/card/schema.json'); console.log(JSON.stringify(s.attributes.number))"` — outputs `{"type":"string","required":true}` with no regex
2. `cd bank-dash-app && npx jest src/services/test/cards.test.ts --no-coverage` — all pass
3. `cd bank-dash-app && npm run test` — full suite green
</verification>

<success_criteria>
- Strapi schema.json "number" attribute has no "regex" key
- Strapi schema.json "number" attribute still has "required": true and "type": "string"
- All existing card service tests pass
- Full frontend test suite is green
</success_criteria>

<output>
After completion, create `.planning/phases/02-implement/02-02-SUMMARY.md`
</output>
